PASO DE LOS FILTROS AL FOOTER:

1) creamos el Componente Footer.
2) le tenemos que pasar los filtros como props para poder usarlos para eso:
APP:
3) EXTRACCION DE LOS DATOS A LA PROP:
a) extraemos los datos del Hook useFilters(), que contiene la logica de los filtros y su definicion como un OBJETO {category, minPrice}
b) le pedimos este objeto en el return de useFilters ==>   return { filters };
c) extraemos y destructuramos el useFilters con los filtros ==>  const { filters } = useFilters();
...** RECORDAR QUE ES UN OBJETO, POR ESO ES ENTRE LLAVES **...

FOOTER:
4) paso como props los filters.
5) en la validacion de las Props, recordar que es un objeto.
6) POR ESTO MISMO QUE ES UN OBJETO:
en el momento de reenderizarlo no me va a dejar, me va a decir que es una colleccion hijos (tiene varios string)
por lo tanto SIEMPRE QUE SEA UN OBJETO USAR: 

      {JSON.stringify(filters, null, 2)}

      convertir a una cadena de texto:
      mostrara filters
      null: es opcional y se utiliza para una función especial de transformación que no estás utilizando, por eso se coloca null.
      
      2:se utiliza para dar formato a la cadena JSON. Al poner 2, estás indicando que deseas que la cadena tenga una apariencia más organizada y legible para los humanos, con una indentación de dos espacios en blanco para cada nivel de anidación 
